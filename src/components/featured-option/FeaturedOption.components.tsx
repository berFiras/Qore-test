import classes from "./FeaturedOption.components.module.scss";

const FeaturedOption = () => {
  return (
    <div className={classes["featured-option"]}>FeaturedOption.components</div>
  );
};

export default FeaturedOption;
